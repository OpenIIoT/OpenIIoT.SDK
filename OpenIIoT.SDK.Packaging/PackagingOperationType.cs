namespace OpenIIoT.SDK.Packaging
{
    public enum PackagingOperationType
    {
        Manifest,
        ExtractManifest,
        Package,
        ExtractPackage,
        Trust,
        Verify
    }
}